name: billing
description: Native IAP billing - subscription + credit wallet system
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: '>=3.16.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  in_app_purchase: ^3.2.0
  supabase_flutter: ^2.3.0
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  networking:
    path: ../networking

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.7
  freezed: ^2.4.5
  json_serializable: ^6.7.1
