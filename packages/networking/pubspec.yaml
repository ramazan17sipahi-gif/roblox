name: networking
description: "Network clients, Supabase wrappers and abstractions."
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  supabase_flutter: ^2.4.0
  models:
    path: ../models

dev_dependencies:
  build_runner: ^2.4.8
